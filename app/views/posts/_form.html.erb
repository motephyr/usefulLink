<%= form_for @post do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited
        this post from being saved:</h2>
      <ul>
        <% @post.errors.full_posts.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
  <%= f.label :網址 %>
  <%= f.text_field :url %>
  <%= f.label :標題 %>
  <%= f.text_field :title %>
  <%= f.label :內容 %>
  <%= f.text_area :description %>
  <%= f.label :類別 %>
  <%= select_tag(:category, options_for_select(['Gem','新聞','教學','討論'])) %>

  <%= f.submit "送出" %>
  </p>
<% end %>
